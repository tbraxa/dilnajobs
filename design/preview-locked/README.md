# Locked DílnaJobs preview (source of truth)

Approved industrial UI: cream `#F2F0EA`, accent `#003DFF`, Satoshi + Inter + JetBrains Mono.

Complete set: `styles.css`, `index.html`, `nabidky.html`, `nabidka.html`, `pro-firmy.html`, `mega-menu.js`, `icons.svg`.

Next.js marketing pages (`/`, `/nabidky`, `/nabidka/*`, `/pro-firmy`) must match these HTML/CSS files structurally 1:1 — not a reinterpretation.

Do not reintroduce `.parts/` fragments or `.github/workflows/assemble-locked-preview.yml` — those were upload artifacts only. `.parts/` stays gitignored.

# Design audit criteria — vs locked SoT `preview-v6-soa/`

**Role:** Design = IA/nav auditor only (no moodboard authorship)  
**SoT:** `enterprise-mvp/preview-v6-soa/` (Tomáš LOCK 2026-09-19)  
**Port target:** Next app (Sol/Opus) — audit Ready preview against SoT, not invent craft

## PASS only if all true

1. **Search hero dominates** `/` and `/nabidky` (Jobs.cz scale) — not buried by split/rail  
2. **Listings** = compensation-first **cards** (Wellfound clarity); NOT 3-col ledger; NOT rejected salary-spine split  
3. **Public chrome** = Vercel/Geist clean (white, 1px `#E5E5E5`, calm density)  
4. **CTA** `#0047FF` (+ darker hover); **gray hover** `#EEEEEE` on nav/chips/rows/controls  
5. **Secondary** = white + **black 1px border** outline  
6. **Charts** (if present): line/area/sparkline + horizontal funnel only — **NO bar/column charts**  
7. Seeded analytics labeled **„Ukázková data“**  
8. UX-NAV-MEGA IA preserved (Nabídky · Průvodce · Pro firmy · Přihlásit se · …)  
9. `/firma` `/ucet` may use left rail; public SERP must not  

## HARD FAIL

- Clone of `www.fairjobs.cz` / Craft / pastel / rejected split moodboard  
- Fake PASS on near-restyles  
- Bar charts in product UI  
- Search not the primary focus on `/nabidky`

## Shot pack when Ready

`soa-hp` · `soa-nabidky` · `soa-drawer` · `soa-firma` · `soa-ucet` · side-by-side vs SoT HTML if needed
